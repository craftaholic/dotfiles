{
  "name": "Custom devcontainer",
  "image": "ghcr.io/craftaholic/devcontainer:latest",

  "containerUser": "dev",
  "remoteUser": "dev",
  "updateRemoteUserUID": false,

  "runArgs": [
//    "--env-file", ".env"
  ],

  "containerEnv": {
    "DOCKER_TLS_CERTDIR": "",
  },

  "remoteEnv": {
    "AWS_PAGER": "" // This is for no pager requires for aws cli
//    "AWS_BEARER_TOKEN_BEDROCK": "${localEnv:AWS_BEARER_TOKEN_BEDROCK}"
  },

  "mounts": [
    // Using variables
    "source=${localEnv:HOME}/.config/opencode/opencode.json,target=/home/dev/.config/opencode/opencode.json,type=bind,readonly",
    "source=${localEnv:HOME}/.config/opencode/oh-my-openagent.json,target=/home/dev/.config/opencode/oh-my-openagent.json,type=bind,readonly",
    "source=${localEnv:HOME}/.config/ocp,target=/home/dev/.config/ocp,type=bind,readonly",
    "source=${localEnv:HOME}/.config/tc-tymekit,target=/home/dev/.config/tc-tymekit,type=bind,readonly",
    "source=${localWorkspaceFolder}/.devcontainer/aws-generated/credentials,target=/home/dev/.aws/credentials,type=bind,consistency=cached"
  ],

  "forwardPorts": []
}
